import type { Metadata } from "next";

/** Shared noindex metadata for authenticated / auth-flow shells. */
export const noIndexMetadata: Metadata = {
  robots: {
    index: false,
    follow: false,
    nocache: true,
    googleBot: {
      index: false,
      follow: false,
      noimageindex: true,
    },
  },
};
